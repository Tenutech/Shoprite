<?xml version="1.0"?>
<ruleset name="CPS Ruleset">
    <arg name="colors" />
    
    <!-- Include the PSR-12 standard -->
    <rule ref="PSR12"/>

    <!-- Ignore specific file for line length rule -->
    <rule ref="Generic.Files.LineLength">
        <properties>
            <property name="lineLimit" value="390" />
            <property name="absoluteLineLimit" value="390" />
        </properties>
    </rule>
</ruleset>